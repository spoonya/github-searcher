export type Inner = {
  children: React.ReactNode;
};

export type Image = {
  src: string;
  alt: string;
};

export type Link = {
  children: React.ReactNode;
  href: string;
};

export type Item = {
  children: React.ReactNode;
  src: string;
  alt: string;
};
