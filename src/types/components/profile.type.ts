export type Profile = {
  children: React.ReactNode;
};

export type ProfileImage = {
  src: string;
  alt: string;
};

export type ProfileLink = {
  children: React.ReactNode;
  href: string;
};

export type ProfileItem = {
  children: React.ReactNode;
  src: string;
  alt: string;
};
